export type Token = {
  token: string;
};

export type UserId = {
  id: string;
};

export type NewPasswordForm = {
  password: string;
  confirmPassword: string;
};

export type ResetPasswordForm = {
  email: string;
};
